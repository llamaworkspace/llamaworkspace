// ToTest: Do tests
describe('getPostConfigForChatId', () => {
  describe('when the chat does not have a linked postConfigVersion', () => {
    it.todo('returns the current version. TODO: Test with many verions!')
  })

  describe('when the chat does have a linked postConfigVersion', () => {
    it.todo('returns the latest version. TODO: Test with many versions!')
  })

  describe('when there are multiple versions', () => {
    describe('when there are multiple versions', () => {
      it.todo('do tests')
    })
  })
})
