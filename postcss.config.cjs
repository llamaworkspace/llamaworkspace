const config = {
  plugins: {
    // Start: Remove is emoji-react-picker is replaced
    'postcss-import': {},
    'tailwindcss/nesting': 'postcss-nesting',
    // End: Remove is emoji-react-picker is replaced
    tailwindcss: {},
    autoprefixer: {},
  },
}

module.exports = config
